# Package
version       = "0.1.0"
author        = "Carlo Capocasa"
description   = "Maildir library and CLI tool"
license       = "MIT"
srcDir        = "src"
bin           = @["maildir"]

# Dependencies
requires "nim >= 2.0.0"
requires "cligen >= 1.6.0"
